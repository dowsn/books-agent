# MCP Agent Configuration Example
# Copy this file to mcp_agent.config.yaml and configure it for your setup

execution_engine: asyncio
logger:
  transports: [console, file]
  level: info
  progress_display: true

mcp:
  servers:
    # Brave Search MCP Server (requires BRAVE_API_KEY in secrets)
    brave-search:
      command: npx
      args:
        - "-y"
        - "@modelcontextprotocol/server-brave-search"
      env:
        XDG_CONFIG_HOME: /tmp/.config
        HOME: /tmp
    
    # Open Library MCP Server (no API key required)
    open-library:
      command: node
      args:
        - "mcps/open-library/build/index.js"
      env:
        XDG_CONFIG_HOME: /tmp/.config
        HOME: /tmp

google:
  # Default Gemini model for the agent
  default_model: "gemini-2.0-flash-exp"
